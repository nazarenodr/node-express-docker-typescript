{
  "name": "backend",
  "version": "1.0.0",
  "scripts": {
    "dev": "docker-compose -f docker-compose-dev.yaml up -d --build --remove-orphans",
    "local": "docker-compose -f docker-compose-local.yaml up -d --build --remove-orphans"
  }
}
